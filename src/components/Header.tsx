import { JSXElement } from "solid-js";

export default function Header(): JSXElement {
    return <header class="h-14 flex items-center">
        <div class="text-lg">Speech Shadowing App</div>
    </header>
}